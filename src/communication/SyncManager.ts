// Sawt Atlas Urgence — Gestionnaire de synchronisation : upload des sessions en attente quand le réseau revient
// Fichier créé le 2026-05-07

export const syncPendingSessions = async (): Promise<number> => 0;

export const queueForSync = async (_sessionId: string): Promise<void> => {};
